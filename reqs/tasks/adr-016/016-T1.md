---
id: task-016-11
parent: adr-016
parent_issue: #19
type: task
title: "[TDD] L1AutomationUseCase: DesignDoc サポート and 初期ラベル付与の拡張"
status: Ready
phase: impl
role: tdd
roadmap: "reqs/context/adr-016/context-map.md"
depends_on: []
issue_id:
---

# [TDD] L1AutomationUseCase: DesignDoc サポート and 初期ラベル付与の拡張

## 1. 目的と背景 (Goal & Context)

- **As-is (現状)**: 現状の `get_labels` は `adr-(\d{3})` のみを正規表現で検索しており、`design-` プレフィックスを処理できない。また、全ての L1 Issue に対して一律で `arch` ラベルを付与する前提となっている。
- **To-be (あるべき姿)**: `L1AutomationUseCase.get_labels` が `adr-XXX` (arch) と `design-XXX` (spec) の両プレフィックスに対応し、適切な初期フェーズラベルと `plan` ラベルを返却する。
- **Design Evidence (設計の根拠)**: ADR-016 「ADR Phase Transition Automation」 4.1. 初期ラベル付与

## 2. 参照資料・入力ファイル (Input Context)

- [ ] `src/issue_creator_kit/usecase/l1_automation_usecase.py`
- [ ] `tests/unit/usecase/test_l1_automation_usecase.py`
- [ ] `reqs/context/adr-016/context-map.md`

## 3. 実装手順と制約 (Implementation Steps & Constraints)

### 3.1. 負の制約 (Negative Constraints)

- [ ] **変更禁止**: `L1AutomationUseCase.execute` のコアロジック（One-ADR Policy 等）は変更しない。
- [ ] **スコープ外**: Issue の作成自体や GitHub API との通信部分は本タスクの範囲外。

### 3.2. 実装手順 (Changes)

- [ ] **ファイル**: `tests/unit/usecase/test_l1_automation_usecase.py`
  - **処理内容**: `design-XXX` 形式の ID に対するテストケースを追加し、Red 状態を確認する。
- [ ] **ファイル**: `src/issue_creator_kit/usecase/l1_automation_usecase.py`
  - **処理内容**: `get_labels` メソッドを修正。
  - **正規表現**: `(?:adr|design)-(\d{3})` を使用して ID をパースする。
  - **ロジック**: プレフィックスが `adr` なら `arch`、`design` なら `spec` を付与し、さらに `plan` と `L1`, `(adr|design):NNN` をリストに含める。
- [ ] **テスト実行**: Pytest を実行し、Green 状態になることを確認する。

### 3.3. 構成変更・削除 (Configuration / Cleanup)

- [ ] **削除**: なし。

## 4. ブランチ戦略 (Branching Strategy)

- **ベースブランチ (Base Branch)**: main
- **作業ブランチ (Feature Branch)**: tdd/task-016-11-l1-automation

## 5. 検証手順・完了条件 (Verification & DoD)

- [ ] **自動テスト**: `pytest tests/unit/usecase/test_l1_automation_usecase.py` がパスすること。
- [ ] **静的解析**: `ruff check .` および `mypy .` がパスすること。
- [ ] **観測される挙動**: `get_labels("design-016")` が `['design:016', 'L1', 'spec', 'plan']` を返すこと。
- [ ] **ファイル状態**: 構文エラーがなく、正規表現が適切にコンパイルされていること。

## 6. 成果物 (Deliverables)

- `src/issue_creator_kit/usecase/l1_automation_usecase.py`
- `tests/unit/usecase/test_l1_automation_usecase.py`
