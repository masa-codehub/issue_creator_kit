---
id: "016-T4"
title: "[TDD Integration] ADR-016: フェーズ遷移自動化の統合検証と監査"
parent: "adr-016"
type: "task"
status: "Ready"
phase: "tdd"
depends_on: ["016-T3"]
issue_id:
---

# 1. Goal & Context

## SMART Goal

ADR-016 の全ての機能（DesignDoc 起票からフェーズ遷移まで）が、統合 テストを通じて期待通りに動作することを検証し、最終的な品質監査（auditing-tdd）を実施する。

## As-is

各コンポーネントの単体テストは完了しているが、実際に `design-doc`  が起票され、複数のタスク完了を経てフェーズが遷移する一連のシナリオ （L1 -> TDD Integration）が検証されていない。

## To-be

`tests/integration/test_adr016_phase_transition.py` において、以下 の統合シナリオがパスすることを確認する：

- `design-doc` 形式のドキュメントがスキャンされ、初期フェーズ `spec` で起票される。
- `integration(spec+audit)` タスクが完了し、上位 Issue のラベルが `tdd` に遷移する。
- 最終的なコード品質とテストカバレッジが `auditing-tdd` をパスする 。

## Design Evidence

- `reqs/design/_approved/adr-016.md`
- `reqs/context/adr-016/context-map.md`

# 2. Input Context

- `tests/integration/test_adr016_phase_transition.py` (新規作成)
- 全実装済みソースコード

# 3. Implementation Steps & Constraints

## Steps

1. `tests/integration/test_adr016_phase_transition.py` を作成し、ドキュメントのスキャン、起票、リレー実行によるラベル遷移の一連のフロ ーをテストする（シナリオテスト）。
2. GitHub API の模擬レスポンスを構成し、`put` によるラベル置換が正 しいペイロードで行われることを検証する。
3. `auditing-tdd` スキルを実行し、実装内容が ADR-016 および仕様と完全に一致していることを監査する。
4. 全てのテストと監査が完了したら、統合ブランチから `main` へのプルリクエストを作成する。

## Constraints

- 実際の GitHub リポジトリへの書き込みは行わず、モック環境で検証す ること。

# 4. Branching Strategy

- **Base Branch**: `main`
- **Feature Branch**: `tdd/integration-adr016`

# 5. Verification & DoD

- [ ] `pytest tests/integration/test_adr016_phase_transition.py` がパスする。
- [ ] `auditing-tdd` レポートで重大な不備が報告されないこと。
- [ ] 統合用の PR が作成され、マージの準備が整っていること。
