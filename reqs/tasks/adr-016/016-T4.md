---
id: task-016-04
parent: adr-016
parent_issue: #19
type: integration
title: "[TDD Integration] ADR-016: フェーズ遷移自動化の統合検証と監査"
status: Ready
phase: audit
role: tdd
roadmap: "reqs/context/adr-016/context-map.md"
depends_on: ["task-016-03"]
issue_id:
---

# [TDD Integration] ADR-016: フェーズ遷移自動化の統合検証と監査

## 1. 目的と背景 (Goal & Context)

- **As-is (現状)**: 各コンポーネント（L1Automation, GitHubAdapter, RelayEngine）の単体テストは完了しているが、実際に `design-doc` が起票され、複数のタスク完了を経てフェーズが遷移する一連のシナリオ（L1 -> TDD Integration）が検証されていない。
- **To-be (あるべき姿)**: 統合シナリオ（起票、タスク完了、ラベル遷移、冪等性）がパスし、最終的なコード品質と仕様の整合性が監査を通じて証明されている。
- **Design Evidence (設計の根拠)**: ADR-016 「ADR Phase Transition Automation」 全体

## 2. 参照資料・入力ファイル (Input Context)

- [ ] `tests/integration/test_adr016_phase_transition.py` (新規作成)
- [ ] `src/issue_creator_kit/` (全ソースコード)
- [ ] `reqs/context/adr-016/context-map.md`

## 3. 実装手順と制約 (Implementation Steps & Constraints)

### 3.1. 負の制約 (Negative Constraints)

- [ ] **変更禁止**: テストのためにプロダクトコード側にテスト専用の分岐を設けない（可能な限り既存のインターフェースを通じて検証する）。
- [ ] **スコープ外**: 本番の GitHub リポジトリへの書き込みは厳禁（すべてモックまたはテスト用スタブを使用する）。

### 3.2. 実装手順 (Changes)

- [ ] **ファイル**: `tests/integration/test_adr016_phase_transition.py`
  - **処理内容**: `design-doc` 形式のドキュメントのスキャンから、初期フェーズ `spec` での起票、そして `integration(spec+audit)` タスクの完了イベントによる `tdd` への遷移を一気通貫で検証するシナリオテストを記述。
- [ ] **監査実行**: `auditing-tdd` スキルを実行。
  - 実装内容が ADR-016 およびコンテキストマップの仮説と完全に一致しているか、例外系（冪等性等）が網羅されているかを監査。
- [ ] **PR作成**: 統合ブランチから `main` へのプルリクエストを作成し、レビューを依頼。

### 3.3. 構成変更・削除 (Configuration / Cleanup)

- [ ] **削除**: なし。

## 4. ブランチ戦略 (Branching Strategy)

- **ベースブランチ (Base Branch)**: main
- **作業ブランチ (Feature Branch)**: tdd/integration-adr016

## 5. 検証手順・完了条件 (Verification & DoD)

- [ ] **自動テスト**: `pytest tests/integration/test_adr016_phase_transition.py` がパスすること。
- [ ] **静的解析**: `ruff check .` および `mypy .` がパスすること。
- [ ] **観測される挙動**: 統合シナリオにおいて、上位 Issue のラベルが初期化から最終フェーズまで期待通りに推移すること。
- [ ] **ファイル状態**: 最終監査レポートで重大な不備が指摘されていないこと。

## 6. 成果物 (Deliverables)

- `tests/integration/test_adr016_phase_transition.py`
- `auditing-tdd` による監査レポート
